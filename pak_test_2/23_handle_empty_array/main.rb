# Initializes an empty array. Checks if the array is empty. Prints "Array is empty" if it's empty; otherwise, prints "Array is not empty".

arr = []

puts arr.empty? ? 'Array is empty' : 'Array is not empty'
